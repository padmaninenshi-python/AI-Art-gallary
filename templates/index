<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>AI Art Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #f0f2f5; }
        .card img { border-bottom: 1px solid #ddd; }
        .gallery-container { margin-top: 30px; }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="text-center mb-5">
            <h1 class="display-4 fw-bold text-primary">AI Art Generator</h1>
            <p class="text-muted">Stability AI se images generate karein</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <form method="POST" class="input-group shadow-sm">
                    <input type="text" name="prompt" class="form-control form-control-lg" placeholder="e.g., A futuristic Ahmedabad city..." required>
                    <button class="btn btn-primary px-4" type="submit">Generate</button>
                </form>
            </div>
        </div>

        <div class="row gallery-container">
            {% for item in gallery %}
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm h-100">
                    <img src="data:image/png;base64,{{ item.image }}" class="card-img-top">
                    <div class="card-body">
                        <p class="card-text small text-muted italic">"{{ item.prompt }}"</p>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>
